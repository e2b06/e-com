import styled from 'styled-components'

export const Container = styled.span`
  align-items: center;
  display: inline-flex;
  margin-right: 0.5rem;

  > svg {
    color: rgb(255, 185, 0);
    margin-right: 0.25rem;
  }
`
